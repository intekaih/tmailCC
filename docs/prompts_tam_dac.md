# BẢNG TỔNG HỢP PROMPTS TÂM ĐẮC TRONG DỰ ÁN tmailCC

Dưới đây là bảng tổng hợp các prompts tâm đắc đã được áp dụng trong suốt quá trình xây dựng, thiết kế giao diện theo phong cách Minimalist Monochrome, sửa lỗi đồng bộ Realtime, tối ưu hóa RLS và triển khai hệ thống lên máy chủ VPS AWS thông qua Cloudflare Tunnel.

Bạn có thể copy bảng Markdown này để dán trực tiếp vào Excel hoặc Google Sheets (Sử dụng tính năng Import hoặc dán trực tiếp).

---

| STT | Prompts | Mô tả ngắn về kết quả đầu ra |
|:---:|---|---|
| **1** | `<role>`<br>`You are an expert frontend engineer, UI/UX designer, visual design specialist, and typography expert. Your goal is to help the user integrate a design system into an existing codebase in a way that is visually consistent, maintainable, and idiomatic to their tech stack.`<br>`Before proposing or writing any code, first build a clear mental model of the current system:`<br>`- Identify the tech stack (e.g. React, Next.js, Vue, Tailwind, shadcn/ui, etc.).`<br>`- Understand the existing design tokens (colors, spacing, typography, radii, shadows), global styles, and utility patterns.`<br>`- Review the current component architecture (atoms/molecules/organisms, layout primitives, etc.) and naming conventions.`<br>`- Note any constraints (legacy CSS, design library in use, performance or bundle-size considerations).`<br>`Ask the user focused questions to understand the user's goals. Do they want:`<br>`- a specific component or page redesigned in the new style,`<br>`- existing components refactored to the new system, or`<br>`- new pages/features built entirely in the new style?`<br>`Once you understand the context and scope, do the following:`<br>`- Propose a concise implementation plan that follows best practices, prioritizing:`<br>`  - centralizing design tokens,`<br>`  - reusability and composability of components,`<br>`  - minimizing duplication and one-off styles,`<br>`  - long-term maintainability and clear naming.`<br>`- When writing code, match the user’s existing patterns (folder structure, naming, styling approach, and component patterns).`<br>`- Explain your reasoning briefly as you go, so the user understands why you’re making certain architectural or design choices.`<br>`Always aim to:`<br>`- Preserve or improve accessibility.`<br>`- Maintain visual consistency with the provided design system.`<br>`- Leave the codebase in a cleaner, more coherent state than you found it.`<br>`- Ensure layouts are responsive and usable across devices.`<br>`- Make deliberate, creative design choices (layout, motion, interaction details, and typography) that express the design system’s personality instead of producing a generic or boilerplate UI.`<br>`</role>`<br><br>`<design-system>`<br>`# Design Style: Minimalist Monochrome`<br>`## Design Philosophy`<br>`### Core Principle`<br>`Reduction to Essence. Minimalist Monochrome strips design down to its most fundamental elements: black, white, and typography. There are no accent colors to hide behind, no gradients to soften edges, no shadows to create false depth. Every design decision must stand on its own merit. This is design as discipline—where restraint becomes the ultimate form of expression...` *(Toàn bộ đặc tả Design System Minimalist Monochrome bao gồm Color Tokens, Serif Typography, Border Radius 0px, Line-Based System, Border Thicken và Inversion effects)*<br>`</design-system>` | Thiết lập hệ thống UI/UX Minimalist Monochrome cho ứng dụng, quy định rõ display tokens (font stacks, border radiuses, spacing, colors), triết lý thiết kế phẳng, góc cạnh sắc nhọn (0px border radius) và các hiệu ứng chuyển đổi trạng thái (inversion) cùng với họa tiết đường kẻ ngang (Horizontal Lines texture). |
| **2** | `"Thiết kế thành phần AccountItem và EmailList hiển thị địa chỉ email. Khi địa chỉ email quá dài, cần giới hạn ký tự (truncate) và thêm padding bên phải để badge thông báo số thư chưa đọc không bị đè lên văn bản. Chia header của danh sách thư thành 2 hàng để tối ưu không gian hiển thị trên thanh bên."` | Khắc phục hoàn toàn lỗi tràn chữ và ghi đè nhãn đếm tin nhắn chưa đọc (unread count badge) trên thanh bên Sidebar khi tên địa chỉ email quá dài, mang lại vẻ ngoài gọn gàng, cân đối. |
| **3** | `"Thêm nút 'Đánh dấu đã đọc tất cả' và nút 'Xóa tất cả' nằm gọn gàng bên phải thanh điều khiển. Nút đánh dấu đã đọc sử dụng icon 2 dấu tích (✓✓), hiển thị mô tả khi di chuột (hover tooltip). Viết hàm xử lý handleMarkAllRead gọi đồng thời API để cập nhật trạng thái is_read = true cho toàn bộ thư của hòm thư hiện tại."` | Triển khai nút hành động thao tác nhanh trên danh sách thư bằng SVG không màu sắc theo đúng quy chuẩn thiết kế tối giản, hỗ trợ chú thích tooltip khi hover và xử lý đồng bộ trạng thái đọc hàng loạt về cơ sở dữ liệu. |
| **4** | `"Xây dựng cơ chế Realtime Subscriptions đồng bộ thư mới giữa Next.js client và Supabase. Đảm bảo khi có sự kiện INSERT trong bảng emails, giao diện tự động cập nhật danh sách thư mà không cần tải lại trang. Tối ưu hóa RLS policies trên bảng emails và accounts để tránh lỗi silent drop của Supabase Realtime đối với cả tài khoản guest và tài khoản đăng nhập."` | Triển khai tính năng tự động nhận email và đếm số thư chưa đọc theo thời gian thực (Realtime), đồng thời cải tổ RLS policies sang dạng subquery-free để tránh lỗi silent drop của Supabase. |
| **5** | `"Viết Dockerfile sử dụng multi-stage build để tối ưu dung lượng của Next.js Standalone mode. Kết xuất output là standalone và cấu hình cổng mặc định 3000. Tạo tệp docker-compose.yml kết nối Next.js container với mạng nội bộ và ánh xạ các biến môi trường cấu hình kết nối tới Supabase."` | Thiết lập Dockerfile & Docker Compose giúp đóng gói ứng dụng gọn nhẹ, độc lập các dependency, dễ dàng xây dựng môi trường staging/production đồng nhất. |
| **6** | `"Cấu hình Nginx làm reverse proxy trên cổng 80 và 443 trỏ về Next.js container (port 3000). Chuyển tiếp toàn bộ kết nối từ HTTP sang HTTPS. Cấu hình các tiêu đề bảo mật (X-Frame-Options, X-Content-Type-Options) và nén Gzip. Sử dụng chứng chỉ SSL tự ký để tương thích với chế độ mã hóa Full SSL của Cloudflare."` | Cài đặt và cấu hình Nginx làm máy chủ chuyển tiếp, mã hóa giao dịch với chứng chỉ SSL cục bộ, đảm bảo bảo mật đường truyền trước khi đi qua Cloudflare CDN. |
| **7** | `"Cài đặt gói cloudflared trên VPS Linux Debian, đăng ký dịch vụ chạy ngầm systemd với token cung cấp từ dashboard Cloudflare Zero Trust. Định tuyến toàn bộ lưu lượng của tên miền tmailcc.app tới cổng local 3000 của Next.js."` | Thiết lập Cloudflare Tunnel thành công, kết nối an toàn máy chủ VPS với mạng toàn cầu của Cloudflare mà không cần mở cổng công cộng 80/443 trên tường lửa của VPS. |
| **8** | `"Bạn là principal software architect, security reviewer và senior full-stack engineer. Hãy thực hiện audit toàn diện dự án tmailCC để đánh giá chất lượng thực tế của codebase ở mức production-ready và mức phù hợp với đồ án cuối kỳ..."`<br>*(Các yêu cầu chi tiết bao gồm audit 18 tiêu chí cốt lõi: Kiến trúc tổng thể, Đối chiếu quy chế thi, App Router, Express Server, Supabase Integration, Developer API, Realtime & Notifications, Clean Code, SOLID/DRY, Bảo mật, Hiệu năng, Docker & Deployment, Dependencies, UX/UI, Tài liệu và Tự động kiểm tra)* | Thực hiện phân tích, đánh giá chi tiết cấu trúc mã nguồn dự án tmailCC theo 18 hạng mục chuẩn công nghiệp và quy chế thi (như Server Actions, RLS, Realtime, bảo mật và hiệu năng) nhằm tìm lỗi tiềm ẩn, code thừa và lập lộ trình cải tiến (roadmap) tối ưu hóa dự án đồ án. |
| **9** | `"Hãy triển khai chức năng Developer API cho tmailCC..."`<br>*(Các yêu cầu bao gồm: Hệ thống API Key băm hash bảo mật, API public version v1 đầy đủ endpoint lấy domain, tạo/xóa account, đọc inbox, wait-otp, usage, cùng với hệ thống Webhook ký HMAC truyền dữ liệu tự động, database migration và giao diện quản lý Developer Settings)* | Thiết kế và triển khai trọn bộ tính năng Developer API cho tmailCC bao gồm hệ thống API Keys phân quyền chi tiết (scopes), các API endpoints v1 cho phép bên thứ ba tương tác, hệ thống webhook tự động gửi sự kiện (email.received, otp.detected) ký HMAC chữ ký bảo mật và giao diện quản lý Developer Settings trực quan. |


